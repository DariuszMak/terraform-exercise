{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run application",
            "type": "debugpy",
            "request": "launch",
            "program": "${workspaceFolder}/src/main.py",
            "cwd": "${workspaceFolder}",
            "envFile": "${workspaceFolder}/.dev.env",
            "args": []
        }
    ]
}